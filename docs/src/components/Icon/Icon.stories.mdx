import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs';
import {Icon} from 'dooboo-ui';
import {IconTemplate} from './IconStories';

<Meta title="Components/Icon" component={IconTemplate} />

# Icon

> Icon component. Icon extends [phosphor-icons](https://github.com/phosphor-icons/homepage) and 29 icons are added ourselves.

## Included icons

> Other then [phosphor-icons](https://github.com/phosphor-icons/homepage) here are 29 more icons.

`EyeSlash`, `Eye`, `XCircleFill`, `X`, `PaperPlaneRight`, `Trash`, `QuestBoxFill`,
`MagnifyingGlass`, `Note`, `List`, `DotsThreeOutlineVertical`, `HouseFill`, `House`,
`UsersFill`, `Users`, `CheckCircleFill`, `CheckCircle`, `ChatCenteredTextFill`, `ChatCenteredText`,
`FileDashed`, `HandWaving`, `RocketLaunch`, `StarAndCrescent`, `CheckCircleFill`, `CheckCircle`,
`NoteFill`, `Note`, `ChatCenteredTextFill`, `ChatCenteredText`.


```tsx
import {Icon} from 'dooboo-ui';

<Icon {...props} />;
```

<Canvas>
  <Story name="Icon" story={IconTemplate} />
</Canvas>

## Props

<ArgsTable of={Icon} />

